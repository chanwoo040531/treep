server:
  port: 8080

service:
  auth:
    url: http://localhost:9090/api/v1/users/info

spring:
  datasource:
    url: jdbc:mysql://mysql:33061/treep
    username: root
    password: 2122
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        show_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  docker:
    compose:
      start:
        log-level: debug
      file: R:\ProjectRepositories\Java\SMF\treep\service\docker-compose.yaml
logging:
  level:
    web: info
    org.springframework:
      boot: info